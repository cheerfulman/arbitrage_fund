# 更新README.md文件，添加curl命令示例

## 1. 问题分析

### 1.1 用户需求
- 将之前编写的curl命令示例追加到README.md文件中
- 确保curl命令示例与API接口文档部分匹配
- 提供完全可用的curl命令，方便用户测试API接口

### 1.2 当前文档结构
- README.md文件中有"7. API接口文档"部分
- 该部分包含了两个API接口的详细说明：
  1. `/api/ai-analyses/{date}` - 获取指定日期AI分析结果
  2. `/api/trigger-analysis` - 手动触发分析任务

## 2. 解决方案

### 2.1 添加curl命令示例
- 在"7. API接口文档"部分的末尾添加curl命令示例
- 为每个API接口提供对应的curl命令
- 确保curl命令格式正确，可直接复制使用

### 2.2 具体修改内容
- 在"7.2.1 获取AI分析结果"接口文档末尾添加curl命令示例
- 在"7.2.2 手动触发分析任务"接口文档末尾添加curl命令示例
- 保持文档结构清晰，易于阅读

## 3. 实施步骤

1. 打开README.md文件
2. 找到"7. API接口文档"部分
3. 在"7.2.1 获取AI分析结果"接口文档末尾添加curl命令示例
4. 在"7.2.2 手动触发分析任务"接口文档末尾添加curl命令示例
5. 保存修改后的文件

## 4. 预期效果

- README.md文件中包含完整的API接口文档和对应的curl命令示例
- 用户可以直接复制curl命令进行测试
- 文档结构清晰，易于理解和使用

## 5. 具体修改

### 5.1 在"7.2.1 获取AI分析结果"接口文档末尾添加：

```bash
# 获取AI分析结果示例
curl -X GET "http://localhost:8000/api/ai-analyses/2026-01-29" -H "Content-Type: application/json"
```

### 5.2 在"7.2.2 手动触发分析任务"接口文档末尾添加：

```bash
# 手动触发分析任务示例
curl -X POST "http://localhost:8000/api/trigger-analysis" -H "Content-Type: application/json"
```

### 5.3 可选：添加日期参数说明

```bash
# 获取其他日期的分析结果示例
curl -X GET "http://localhost:8000/api/ai-analyses/2026-01-20" -H "Content-Type: application/json"
```